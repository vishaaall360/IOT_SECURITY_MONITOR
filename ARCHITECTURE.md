# System Architecture

The IoT Smart Security Monitoring System follows a client-server architecture.

## Components
- IoT Sensor Simulator (Device Layer)
- Flask Server (Processing Layer)
- Detection Engine (Security Layer)
- Web Dashboard (Presentation Layer)

## Architecture Flow
Sensor → Server → Detection Engine → Alerts → Dashboard → Logs
